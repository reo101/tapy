# BetterDiscord plugin for Tapy

## Building

1. Run `yarn install` to install the dependencies

2. Run `yarn run build` to build the plugin

3. Copy `dist/tapy.plugin.js` into your BetterDiscord plugins directory
